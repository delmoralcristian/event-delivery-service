<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [correlationId=%X{correlationId:-—} eventId=%X{eventId:-—} clientId=%X{clientId:-—}] - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.delmoralcristian" level="DEBUG"/>
    <logger name="org.hibernate.orm.connections.pooling" level="WARN"/>
</configuration>
